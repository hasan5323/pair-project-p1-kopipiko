
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit transaction</title>
    <style>
      body {
        font-size: 17px;
      }

      .others {
        margin-top: 20px;
        display: flex;
        flex-direction: column;
        text-align: left;
      }

      .additem {
        margin-top: 20px;
        display: flex;
        flex-direction: column;
        text-align: left;
      }
      .container {
        display: flex;
        flex-direction: column;
        text-align: left;
      }
    </style>
  </head>
  <body>
    <nav>
      <button><a href="">landing</a></button>
      <button><a href="">add transaction</a></button>
    </nav>
    <h1>Edit Transaction</h1>
    <div class="container">
      <form action="/add" method="post">
        <div>
          <p>Transaction Id: <%= result.id %></p>
          <p>User Id: <%= result.userId %></p>
        </div>
        <label for="pickUpLocation">Pick Up Location</label><br />
        <input type="text" name="pickUpLocation" id=""  value="<%= result.pickUpLocation %>" required /><br />
        <label for="dropLocation">Drop Off Location</label><br />
        <input type="text" name="dropLocation" id="" value="<%= result.dropLocation %>" required /><br />
        <label for="bill">Bill</label><br />
        <input type="number" name="bill" value="<%= result.bill %>" /><br /><br />
        <% transactionItems.forEach((el)=> { %>
        <label for="name<%= el.id %>"><%= el.name %></label><br />
        <input type="text" name="name<%= el.id %>" value="<%= el.name %>" /><br />
        <label for="name<%= el.id %>"><%= el.name %></label><br />
        <input type="text" name="name<%= el.id %>" value="<%= el.name %>" /><br />
        <% }) %>
        <button type="submit">Update Transaction</button>
      </form>
    </div>
  </body>
</html>
